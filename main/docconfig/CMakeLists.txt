#
#  docconfig holds the docbuilder.tcl metadata (config, part/section intros)
#  read directly from the source tree by docbuild; nothing is built or
#  installed here (the Makefile.am only had EXTRA_DIST).
#
