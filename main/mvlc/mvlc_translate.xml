<!-- manpage 1daq -->
<refentry id='mvlcgen_man'> 
    <refmeta>
        <refentrytitle id='mvlcgen_man_title'>mvlcgenerate</refentrytitle>
        <manvolnum>1daq</manvolnum>
    </refmeta>
    <refnamediv>
        <refname>mvlcgenerate</refname>
        <refpurpose>Generate config files for the mvlc fribdaq-readout program</refpurpose>
    </refnamediv>
    <refsynopsisdiv>
        <cmdsynopsis>
            <command>
mvlcgenerate <optional><option>--output</option>=<replaceable>outfile</replaceable></optional> 
    <replaceable>infile</replaceable>
            </command>
        </cmdsynopsis>
    </refsynopsisdiv>
    <refsect1>
        <title>DESCRIPTION</title>
        <para>
            The Mesytec MVLC controller is a list driven VME controller that can be connected to NSCLDAQ
            either via a USB or an Ethernet connection.  FRIB has contributed the program <filename>fribdaq-readout</filename>
            that can setup the MVLC to contribute data to an FRIB/NSCLDAQ ringbuffer.  At the FRIB, this program is 
            located in:
            <filename>/usr/opt/mesytec-mvlc/<replaceable>version</replaceable>/bin/frbidaq-readout</filename>
            where <replaceable>version</replaceable> is a software version of the MVLC driver software.  
            <command>ls /usr/opt/mesytec-mvlc</command> to see the available versions.  We suggest you use the most
            recent (highest) version number.
        </para>
        <para>
            The <filename>fribdaq-readout</filename> program requires a configuration file in YAML 
            (yet another markup language) format to describe the experiment.  <filename>mvlcgenerate</filename>
            takes a VMUSBReadout <filename>daqconfig.tcl</filename> and translates it into the YAML expected by 
            <filename>fribdaq-readout</filename>
        </para>
        <para>
            The only required command line value is the name of the VMUSBReadout configuration file to be translated.
            By default, the output filename will be the same as the input filename but with the file type changed to 
            <literal>.yaml</literal>.  For example, the command line:
        </para>
        <informalexample>
        <programlisting>
$DAQBIN/mvlcgenerate daqconfig.tcl
        </programlisting>
        </informalexample>
        <para>
            Will produce the file <filename>daqconfig.tcl</filename>
        </para>
        <para>
            The <option>--output</option> allows you to explicitly specify the full output filename e.g.
        </para>
        <informalexample>
            <programlisting>
$DAQBIN/mvlcgenerate --output=myexperiment.yaml daqconfig.tcl
            </programlisting>
        </informalexample>
        <para>
            translates the <filename>daqconfig.tcl</filename> into  the file <filename>myexperiment.yaml</filename>
        </para>
    </refsect1>
    <refsect1>
        <title>Differences from VMUSBReadout</title>
        <para>
            There are some differences between the experiments that can be generated by <filename>mvlcgenerate</filename>
            and those supported by VMUSBReadout.  These are enumerated here:
            <orderedlist>
                <listitem>
                    <para>
                        When interpreting the daqconfig script a Tcl interpreter is used, as it is by 
                        <filename>VMUSBReadout</filename>.  Additonally the environment variable 
                        <literal>MVLC_TRANSLATOR</literal>.
                    </para>
                </listitem>
                <listitem>
                    <para>
                        The only trigger supported by the <command>stack</command> are <literal>nim1</literal> and 
                        <literal>scaler</literal>  the <literal>nim1</literal> trigger is the event stack which is 
                        triggered by the NIN input 1 on the MVLC module.  The <option>-delay</option> is supported for
                        that stack.  The <literal>scaler</literal> trigger is a periodic stack that is run every two seconds
                        and is intended to readout scaler data.  The <option>-period</option> is accepted but ignored.
                        Interrupt driven stacks are not supported.
                    </para>
                </listitem>
                <listitem>
                    <para>
                        The CAEN 32 channel digitizers are supported but, due to the non-interactive nature of the translator, 
                        the actual CAEN digitizer type cannot be determined at run-time.  The additional configuration option:
                        <option>-type</option> can be specified with the value <literal>adc</literal> <literal>tdc</literal>
                        or <literal>-tdc</literal> to specificy which device speicific initializations must be done.  If not
                        specified, the default value is <literal>adc</literal>.  
                        Consider the following code snippet that determines if the mvlcgenerate is being used and
                        defines a module to be  a tdc if so:
                        <informalexample>
                            <programlisting>
set istranslator [array names ::env MVLC_TRANSLATOR]
...
adc create tdc -base 0x12340000
if {$istranslator ne ""} {
    adc config tdc -type tdc
}                           
... 
                            </programlisting>
                        </informalexample>
                    </para>
                </listitem>
                <listitem>
                    <para>
                        The Mesytec MDPP digitizer commands are supported, however the handling of the
                        <option>-triggersource</option> and <option>-triggeroutput</option> values is slightly
                        different.  The value <literal>0x400</literal> was a special value that did not modify the
                        module's current settings.  This is not supported, the full, desired trigger value must be specified.
                        The <option>-printregisters</option> was a debugging option that produced a detailed dump of the 
                        device registers after initialization.  This is not supported.  Finally, the 16 input
                        qdc device <command>mdpp16qdc</command> was not documented but was present and supported by
                        the translator.
                    </para>
                </listitem>
                <listitem>
                    <para>
                        The CAEN Multihit TDC modules are supported, however you must specify the model used.  This
                        is done with the <option>-model</option> configuration option which can be one of
                        <literal>V1190A</literal>, <literal>V1190B</literal>, <literal>V1290A</literal> or 
                        <literal>V1290N</literal>.  IF <option>-model</option> is not specified, it defaults to 
                        <literal>v1190A</literal>
                    </para>
                </listitem>
                <listitem>
                    <para>
                        Support for the SIS3316 was added to the VMUSBReadout as of NSCLDAQ version 12.1 but is not supported 
                        by the translator at this time because its initialization is highly interactive with them module.
                    </para>
                </listitem>
            </orderedlist>
        </para>
    </refsect1>
    <refsect1>
        <title>FILES</title>
        <itemizedlist>
            <listitem>
                <para>
                    <filename>$DAQSHARE/mvlc/triggeronly.yaml</filename> - is a template  yaml configuration
                    file with the appropriate trigger setup and a marker indicating where to modify the event stack 
                    delay.
                </para>
            </listitem>
        </itemizedlist>
    </refsect1>
</refentry>
<!-- /manpage -->

<!-- chapter user-guide -->
<chapter><title>mvlcgenerate and using the Mesytec MVLC VME controller </title>
    <para>
        The Mesytec MVLC is an intelligent VME crate controller.  Like the VMUSB, it is capable of autonomously
        executing a stack (list) of operations in response to a trigger. What distinguishes the MVLC from the VMUSB,
        is:
        <itemizedlist>
            <listitem><para>
                The ability to perform complex trigger decisions to trigger stack execution.
            </para></listitem>
            <listitem><para>
                Additional stack operations that allow more complex VME operations to be performed in stacks.
            </para></listitem>
            <listitem><para>
                An Ethernet interface in <emphasis>addition</emphasis> to the USB interface provided by the VMUSB.
            </para></listitem>
        </itemizedlist>
    </para>
    <para>
        While Mesytec does not provide sufficient documentation for fully homegrown support of the MVLC, the 
        software support in their github repository 
        (<ulink url='http://github.com/flueke/mesytec-mvlc'>http://github.com/flueke/mesytec-mvlc</ulink>) for the 
        user mode does provide a skeleton for a readout program.   We have forked that repository, and created a
        modified version of that readout program and contributed it back to Mesytec.
    </para>
    <para>
        The framework we adapted requires a static  configuration file which is read <emphasis>once</emphasis>
        when the program starts.  This configuration file desribes trigger conditions, initialization operations and
        stacks to execute in response to triggers.
    </para>
    <para>
        At the FRIB in the bookworm and later containers, the driver software, along with our readout program,
        is installed in <filename>/usr/opt/mesytec-mvlc/<replaceable>version</replaceable></filename>.  Where
        <replaceable>version</replaceable> is a version number such as <literal>0.1.0</literal>.  Within that directory 
        tree, the readout program is in <filename>bin/fribdaq-readout</filename>.
    </para>
    <para>
        Coupled with this program, FRIB/NSCLDAQ 12.2, and later, provides a program 
        <filename>$DAQBIN/mvlcgenerate</filename>, which can translate many VMUSBReadout daqconfig.tcl configuration
        files into the configuration files expected by <filename>fribdaq-readout</filename>
    </para>
    <para>
        So, to summarize,  to use the Mesytec MVLC with FRIB/NSCLDAQ you will need:
        <orderedlist>
            <listitem>
                <para>
                    An installed version of the Mesytec MVLC driver software buit with support for
                    FRIB/NSCLDAQ readout (more about how to get that from scratch later in this chapter).
                </para>
            </listitem>
            <listitem>
                <para>
                    A VMUSB <filename>daqconfig.tcl</filename> with devices supported by the
                    <filename>mvlcgenerate</filename> program.
                </para>
            </listitem>
            <listitem>
                <para>
                    Version 12.2 of FRIB/NSCLDAQ or higher, which provides the <filename>mvlcgenerate</filename>
                    program.
                </para>
            </listitem>
        </orderedlist>
        
    </para>
    <section>
        <title>Preparing your experiment to use the MVLC.</title>
        <para>
            Use <link linkend='mvlcgen_man' endterm='mvlcgen_man_title' /> to translate a <filename>daqconfig.tcl</filename>
            written for the VMUSB. This may require small modifications, however those modifications can be made in a way
            that the resulting configuraion file can be used with both the MVLC and the VMUSB controllers.
        </para>
        <para>
            Run <filename>fribdaq-readout</filename> specifying the configuration file that <filename>mvlcgenerate</filename>
            created.     See <link linkend='fribdaq_readout_man' endterm='fribdaq_readout_man_title'/> to see how to use
            this program.  Note that while this program is not yet supported by the ReadoutShell GUI, or managed expermiment,
            support for both is planned.
        </para>
    </section>
    <section>
        <title>Reference manual for <filename>fribdaq-readout</filename></title>
        <refentry id='fribdaq_readout_man'>
            <refmeta>
                <refentrytitle id='fribdaq_readout_man_title'>fribdaq-readout</refentrytitle>
                <manvolnum>1mesytec-mvlc</manvolnum>
            </refmeta>
            <refnamediv>
                <refname>fribdaq-readout</refname>
                <refpurpose>Read data from a Mesytec MVLC interface to a FRIB/NSCLDAQ ringbuffer</refpurpose>
            </refnamediv>
            <refsynopsisdiv>
                <cmdsynopsis>
                    <command>
$MVLCROOT/bin/fribdaq-readout <replaceable>options...</replaceable> <replaceable>configFile</replaceable>
                    </command>
                </cmdsynopsis>
            </refsynopsisdiv>
            <refsect1>
                <title>DESCRIPTION</title>
                <para>
                    This program reads data from a single Mesytec MVLC controller into a ringbuffer.
                    Note that this program is <emphasis>not</emphasis> part of FRIB/NSCLDAQ but requires
                    an installation of the Mesytec mvlc userspace driver.  The environment variable
                    <literal>MVLCROOT</literal> in the command above, is defined by <emphasis>you</emphasis>
                    to point to the top level directory of this installation.  At the time I'm writing this,
                    that is <filename>/usr/opt/mesytec-mvlc/0.1.0</filename> at the FRIB or using the FRRIB
                    containerized bookworm environment.
                </para>
                <para>
                    The <replaceable>configFile</replaceable> on the command line is the output of 
                    <link linkend='mvlcgen_man' endterm='mvlcgen_man_title' /> when run on a VMUSBReadout
                    daqconfig file.
                </para>
            </refsect1>
            <refsect1>
                <title>OPTIONS</title>
                <para>
                    Note that in the list of options below, one and only one of the following options must
                    be provided to describe the MVLC from which data will be collected:
                    <itemizedlist>
                        
                        <listitem>
                            <para>
                                <option>--mvlc-eth</option> Specifies the connection will be Ethernet.
                            </para>
                        </listitem>
                        <listitem>
                            <para>
                                <option>--mvlc-usb</option> specifies the first MVLC device found on the USB
                                system will be used (suitable when there's only one device present).
                            </para>
                        </listitem>
                        <listitem>
                            <para>
                                <option>--mvlc-usb-index</option>  Specifies that a specific index in the enumerated
                                list of MVLC devices attached to the USB be used.  This is not really safe, in my opinion
                                as it relies on a predictable enumeration by linux.
                            </para>
                        </listitem>
                        <listitem>
                            <para>
                                <option>--mvlc-usb-serial</option> Specifies the serial number string of a specific
                                MVLC that is attached to the USB.  This is the safest option to use when you are
                                selecting from more than one attached MVLC.
                            </para>
                        </listitem>
                    </itemizedlist>
                </para>
                
                <para>
                    Note that the crate configuration file can also specify the MVLC to use however as that
                    comes from a template file used by <filename>mvlcgenerate</filename> it, normally, should
                    be overidden by one o fthe options above.
                </para>

                <variablelist>
                    <varlistentry>

                            <term><option>-?</option>, <option>-h</option>, <option>--help</option></term>
                            <listitem>
                                <para>
                                    Display brief usage information in case you can't remember how to run
                                    the program.
                                </para>
                            </listitem>
                    </varlistentry>
                    <varlistentry>
                            <term><option>--mvlc-eth</option> <replaceable>hostname</replaceable></term>
                            <listitem>
                                <para>
                                    mvlc ethernet hostname (overrides CrateConfig).  
                                </para>
                            </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term> <option>--mvlc-usb</option></term>
                        <listitem>
                            <para>
                                Connects to the first MVLC in the USB enumeration of devices.  This is simplest
                                to use when only one MVLC is attached to the host system via USB.
                            </para>
                        </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term> <option>--mvlc-usb-index</option> <replaceable>index</replaceable></term>
                        <listitem>
                            <para>
                                Connect to the mvlc with the given usb device index (overrides CrateConfig).
                                At system startup, and when a USB device is plugged in, or unplugged, linux
                                creates an enumeration of devices (think of it as an array of device types and
                                their connection specifications).  This option allows you to  connect to a specific
                                MVLC by where it appears in this enumeration. Imagine the array described above
                                with all non MVLC devices removed, the <replaceable>index</replaceable>
                                parameter is the index into this reduced array that specifies the MVLC.
                                to use.
                            </para>
                            <para>
                                If it were me, I'd use <option>--mvlc-usb-serial</option> rather than this option.
                            </para>
                        </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term><option>--mvlc-usb-serial</option> <replaceable>serial</replaceable> </term>
                        <listitem>
                            <para>
                                Connect to the mvlc with the given usb serial number (overrides CrateConfig).
                                Each MVLC has a serial number string that is on the module.  Supplying one of
                                those strings as the <replaceable>serial</replaceable> parameter of the
                                option selects the MVLC module with the matching serial number string.
                            </para>
                        </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term><option>--init-only</option> </term>
                        <listitem>
                            <para>
                                Run the DAQ init sequence and exit
                            </para>
                        </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term><option>--ignore-vme-init-errors</option> </term>
                        <listitem>
                            <para>
                                Ignore VME errors during the DAQ init sequence
                            </para>
                        </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term><option>--ring</option> <replaceable>ring</replaceable> </term>
                        <listitem>
                            <para>
                                Ring buffer name.  The name of the ring buffer to which the program will
                                supply data.  The <replaceable>ring</replaceable> parameter must be the
                                name, not URI of the ring buffer, as ring buffer producers can only produce
                                to local ringbuffers.
                            </para>
                            <para>
                                If not provided, the ring name is the logged in user name.
                            </para>
                        </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term><option>--sourceid</option> <replaceable>sourceid</replaceable> </term>
                        <listitem>
                            <para>
                                When a timestamp extractor is provided, 
                                (see <option>--timestamp-library</option> below), this sets the source id in the
                                body headers of ring items submitted to the ring.  This option, in combination
                                with <option>--timstamp-library</option> allows for data from this source to be
                                merged with other sources using the FRIB/NSCLDAQ event builder.
                            </para>
                        </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term><option>--timestamp-library</option> <replaceable>shared-object</replaceable> </term>
                        <listitem>
                            <para>
                                Time stamp shared library file.   <replaceable>shared-object</replaceable> is a
                                shared object library that contains code that knows how to extract a timestamp
                                from each physics event body.  See 
                                <link linkend='fribdaq_readout_tsextract_sec' endterm='fribdaq_readout_tsextract_title' />
                                to learn how to write a timestamp extractor and build it into a suitable
                                library.
                            </para>
                            <para>
                                Note that you may need to make it clear which directory the timestamp 
                                extractor lives in due to the normal shared library load paths.
                                For example <filename>myextractor.so</filename> may fail to load while
                                <filename>./myextractor.so</filename> will succeed.
                            </para>
                        </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term><option>--debug</option> </term>
                        <listitem>
                            <para>
                                Enable debug logging. Useful for me but not for you.  Disabled by default.
                            </para>
                        </listitem>
                    </varlistentry>
                    <varlistentry>
                        <term><option>--trace</option> </term>
                        <listitem>
                            <para>
                                Enable trace logging.  Useful for me but not for you.  Disabled by default.
                            </para>
                        </listitem>
                    </varlistentry>
                </variablelist>
            </refsect1>
            <refsect1>
                <title>Commands</title>
                <para>
                    The <filename>fribdaq-readout</filename> is controlled by commands that are accepted on
                    its stdin.  These commands are complmetely compatible with t
		    he standard FRIB/NSCLDAQ readout command sets and, like all
		    FRIB/NSCLDAQ readout command interpreters run inside
		    an event driven Tcl intpereter making it compatible with
		    the ReST server and ReadoutShell.
                </para>

            </refsect1>
            <refsect1 id='fribdaq_readout_tsextract_sec'>
                <title id='fribdaq_readout_tsextract_title'>TIMESTAMP EXTRACTION PLUGINS</title>
                <para>
                    In order to be used with the FRIB/NSCLDAQ event builder, timstamps must be 
                    extracted from events.  This is done by supplying a timstamp extractor
                    shared object with the <option>--timestamp-library</option>.  This section
                    desribes the requirements of a timstamp extractor and how to build it.
                </para>
                <para>
                    Let's start by looking at a silly example, buliding it and describing how to make it
                    useful.  The silly example will just return an incrementing timestamp:
                </para>
                <example>
                    <title>Silly timestamp extractor for fribdaq-readout</title>
                    <programlisting>

// Filename: tsextract.cpp
#include &lt;fribdaq/parser_callbacks.h&gt;  <co id='fribdaqts.includes' />
#include &lt;stdint.h&gt;

// Timestamp extractors must have C linkage:
extern "C" {                                 <co id='fribdaqts.clinkage' />
    /** Sample timestamp extractor
        normal extractors need to look at the data which are
        in the module structs
     */
    uint64_t                                 <co id='fribdaqts.signature' />
    extract_timestamp(unsigned numModules, const mesytec::mvlc::readout_parser::ModuleData* event) {
        static uint64_t timestamp = 0;

        return timestamp++;                 <co id='fribdaqts.return' />
    }
}

                    </programlisting>
                </example>
                <para>
                    Let's first tear this apart bit by bit, and then show how to build it.  Then we'll
                    describe what we need to do to make a more intersting timestamp extractor.
                </para>
                <calloutlist>
                    <callout arearefs="fribdaqts.includes">
                        <para>
                            The first of these inlcudes is needed to define the struct that contains the
                            event, passed into us.  The second, to define <type>uint64_t</type> an
                            unsigned 64 bit integer into which we'll put the timsetamp.
                        </para>
                    </callout>
                    <callout arearefs="fribdaqts.clinkage">
                        <para>
                            The <filename>fribdaq-readout</filename> program has to locate the extractor
                            from within the shared library.  C++ symbol names for functions get 
                            <firstterm>mangled</firstterm> by the parameters and their types to support
                            overloading.  This makes it hard to predict the actual name of a fuction.
                            the <literal>extern "C"</literal> block causes the compiler to, among other things,
                            disable this function name mangling so that the name of a function can be
                            easily located within a library.  The actual mangling is not part of the C++ standard
                            and may, not only vary from compiler to compiler but from compiler version to compiler version. 
                        </para>
                        <para>
                            Timstamp extractor functions must be defined in side a <literal>extern "C"</literal>
                            block to turn off mangling of their names.
                        </para>
                    </callout>
                    <callout arearefs='fribdaqts.signature'>
                        <para>
                            Timestamp extraction functions must return a <type>uint64_t</type> and be named
                            <literal>extract_timestamp</literal>.   When we make this function more useful,
                            we will say more about the parameters passed into this function.
                        </para>
                    </callout>
                    <callout arearefs="fribdaqts.return">
                        <para>
                            The job of the extractor is to find the timestamp in the event, and return it as a
                            <type>uint64_t</type>.  In the case of this silly example, we just defined a 
                            static variable that increments each time we are called and returned the value
                            of this variable prior to incrementing it.
                        </para>
                    </callout>
                </calloutlist>
                <para>
                    Before turning to making this a useful extractor, let's look at how to create the shared image.
                    This compilation command will do that:
                </para>
                <informalexample>
                    <programlisting>
g++ -otsextract.so -fPIC -shared -I$MVLCROOT/include tsextract.cpp
                    </programlisting>
                </informalexample>
                <para>
                    The <option>-share4d</option> and <option>-fPIC</option> are required when building
                    shared images.  In the <option>-I</option> we assume that <literal>MVLCROOT</literal>
                    is an evironment variable you have pointed to the root of a mesytec-mvlc intallation.
                    At the FRIB this might translate to <filename>/usr/opt/mesytec-mvlc/0.1.0</filename>.
                    This needed to define the structure that is passed to the extractor.
                </para>
                <para>
                    You might well  ask why we bothered to use the C++ compiler rather than the C compiler
                    (which would remove the need for an <literal>extern "C"</literal>).  This is because C 
                    does not have namespaces, and is allowed to pad structures differently than C++ and still
                    be standard compliant. The <type>mesytec::mvlc::readout_parser::ModuleData</type> is
                    defined in nested namespaces and compiled by C++
                </para>
                <para>
                    Let's modify this extractor to take the first 64 bits of the event as the timestamp. 
                    For now we won't worry  about how the timestamp was written to the data, just that the
                    first 64 bit word is the timestamp.  Always.
                </para>
                <para>
                    To do this we need to know how the event is passed to us. Data are read from the 
                    MVLC In 'modules'.  This is because the mdaq program the MVLC support software was originally
                    written for has you adding hardware modules to the readout.  The parameters passed to the
                    timstamp extractor reflects this as well.  The <parameter>numModules</parameter> is the
                    number of parameters that were read.  The <parameter>event</parameter> points to an array of
                    <type> mesytec::mvlc::readout_parser::ModuleData</type>.  Each element of that array
                    describes a module and provides its data.
                </para>
                <para>
                    <type> mesytec::mvlc::readout_parser::ModuleData</type> is a struct that has the following
                    elements:
                </para>
                <variablelist>
                    <varlistentry>
                            <term><type>DataBlock</type> 	<structfield>data</structfield></term>
                            <listitem>
                                <para>
                                    Describes the data, in practice, this is the only field we need in each
                                    module.
                                </para>
                            </listitem>
                    </varlistentry>
                    <varlistentry>
                            <term><type>uint32_t</type> <structfield>prefixSize</structfield></term>
                            <listitem>
                                <para>
                                    Data consist of a statically allocated prefix followed by a dynamically
                                    allocated data body.  This indicates the size of the prefix.  In our
                                    case the entire data block is the chunk of the event we care about.
                                </para>
                            </listitem>
                    </varlistentry>
                    <varlistentry>
                            <term><type>uint32_t</type>	<structfield>dynamicSize</structfield></term>
                            <listitem>
                                <para>
                                    Size of the dynamically allocated chunk.  We normally don't need this.
                                </para>
                            </listitem>
                    </varlistentry>
                    <varlistentry>
                            <term><type>uint32_t</type> <structfield>suffixSize</structfield></term>
                            <listitem>
                                <para>
                                    Size of the suffix chunk.  We don't need this.
                                </para>
                            </listitem>
                    </varlistentry>
                    <varlistentry>
                            <term><type>bool</type> <structfield>hasDynamic</structfield></term>
                            <listitem>
                                <para>
                                    True if there is a dynamic chunk.  We normally don't need to worry about
                                    this field.
                                </para>
                            </listitem>
                    </varlistentry>
                </variablelist>
                <para>
                    The important fieled is the data field.  It is a <type>mesytec::mvlc::readout_parser::DataBlock</type> struct which
                    has the following fields:
                </para>
                <variablelist>
                    <varlistentry>
                            <term><type>const uint32_t*</type> <structfield>data</structfield></term>
                            <listitem>
                                <para>
                                    Pointer to the actual event data.
                                </para>
                            </listitem>
                    </varlistentry>
                    <varlistentry>
                            <term><type>uint32_t</type> <structfield>size</structfield></term>
                            <listitem>
                                <para>
                                    Size in units of uint32_t  of the data chunk.
                                </para>
                            </listitem>
                    </varlistentry>
                </variablelist>
            
            <para>
                Note that all of these definitions are in the <filename>fribdaq/parser_callbacks.h</filename>
                which is installed when the Mesytec software is installed with <filename>fribdaq-readout</filename>
                enabled.
            </para>
            <para>
                The explanation above should make it pretty clear how to write a timestamp extractor where
                the timestamp is in the first 64 bits of the event:
            </para>
            <example>
                <title>Extracting a timestamp from the first 64 bits of MVLC data</title>
                <programlisting>
#include &lt;fribdaq/parser_callbacks.h&gt; 
#include &lt;stdint.h&gt;

// Timestamp extractors must have C linkage:
extern "C" {                                
    /** Sample timestamp extractor
        normal extractors need to look at the data which are
        in the module structs
     */
    uint64_t                             
    extract_timestamp(unsigned numModules, const mesytec::mvlc::readout_parser::ModuleData* event) {
        const uint64_t* pTs = reinterpret_cast&lt;const uint64_t*&gt;(event->data.data); <co id='fribdaqts.ptr' />
        return *pTs; 
    }
}

                </programlisting>
            </example>
        <calloutlist>
            <callout arearefs='fribdaqts.ptr'>
                <para>
                    This line of code does all the work.  It takes the pointer to the first block of event
                    data and converts it into a  <type>const uint64_t*</type> pointer.
                    This pointer, points to the first 64 bits of the event. Dereferencing it results in the
                    event timestamp.
                </para>
            </callout>
        </calloutlist>
        <para>
            You can use this as a template to build a timestamp extractor that matches the data for your
            event.
        </para>
        </refsect1>
        </refentry>
    </section>
    <section>
        <title>RING ITEMS PRODUCED</title>
        <para>
            The <filename>fribdaq-readout</filename> program produces normal FRIB/NSCLDAQ ring items.  The
            format of the ring items, depends on the version of the DAQ the program is linked to.  If a timestamp
            extractor is specified, the ring items will have non-empty body headers.  If not, they will have
            empty body headers.
        </para>
        <para>
            Prior to all state change items, a format item is emitted.   This format item will reflect the
            format of the underlying version of FRIB/NSCLDAQ.  Normally, this will be 12.0 or higher.
        </para>
        <para>
            For each event, a <literal>PYSICS_EVENT</literal> item is emitted.  As you have seen in the previous
            section, events can be segmented.  The data from each segment are concatenated to form the body 
            of the physics event.
        </para>
        <para>
            Scaler items are produced whenever the periodic stack produces data.  As with the VMUSBReadout, 
            elapsed run time data are produced from an internal timer.   In general, this timer will have a finer
            granularity than a second, so you must take into account the divisor field of these items, if you
            are accessing the items directly.  The unified format library is the recommnded way to access data,
            this will compute the elapsed times from the time elements and divisors transparently.
        </para>
        <para>
            The MVLC Scaler readout is purely time based and, therefore, there is not a final scaler item at the
            end of the run.
        </para>
        <para>
            The current version does not produce <literal>PHYSICS_EVENT_COUNT</literal> items. This is planned for
            a later release of the software.
        </para>
    </section>
    <section>
        <title>BUILDING mesytec-mvlc WITH THE OPTIONAL <filename>fribdaq-readout</filename> PROGRAM</title>
        <para>
            If you are using a version of FRIB/NSCLDAQ 12.1 or higher, or at the FRIB in the 
            containerized environment, The <filename>/usr/opt/</filename> filesystem for the <literal>bookworm</literal>
            container will include at least one version of the mesytec mvlc support software that includes 
            <filename>fribdaq-readout</filename>, and you can ignore the remainder of this section.
        </para>
        <para>
            If you are using a native version of FRIB/NSCLDAQ 11.0 and higher, we encourage you to use a containerized
            environment so that you can ignore this section.
        </para>
        <para>
            If you are stubborn, then this section describes how to build the Mesytect-mvlc software so
            that it includes the <filename>fribdaq-readout</filename> program and the appropriate headers
            to allow you to build timestamp extractors.
        </para>
        <para>
            Until our contribution of <filename>fribdaq-readout</filename> is merged into a release, you'll need
            to do the following:
        </para>
        <orderedlist>
            <listitem>
                <para>
                    Clone the <ulink url='https://github.com/flueke/mesytec-mvlc'>https://github.com/flueke/mesytec-mvlc</ulink>
                    repository.
                </para>
            </listitem>
            <listitem>
                <para>
                    Checkout the fribdaq-readout branch. This is necessary as long as the main branch does not include 
                    the <filename>extras/frbdaq</filename> directory.
                </para>
            </listitem>
            <listitem>
                <para>
                    Create a direcory in which you will do the build.
                </para>
            </listitem>
            <listitem>
                <para>
                    Use <command>cmake</command> to generate the Makefiles in the build directory.  You will want
                    to specify <command>-DCMAKE_INSTALL_PREFIX=<replaceable>installation-root</replaceable></command>
                    and <command>-DNSCLDAQ_ROOT=<replaceable>install-root-of-nscldaq</replaceable></command>
                    Where <replaceable>installation-root</replaceable> is where you want the software installed,
                    and <replaceable>install-root-of-nscldaq</replaceable> is the installation directory for the
                    verison of FRIB/NSCLDAQ you want to build against.  12.0 or higher is recommended though it 
                    might work with 11.x as well.
                </para>
            </listitem>
            <listitem>
                <para>
                    <command>make</command> to build the software. If you have a multicore system, you can speed this
                    up using the <option>-j</option> to specify a parallel make.
                </para>
            </listitem>
            <listitem>
                <para>
                    Optionally run the unit tests: <command>make test</command>.
                </para>
            </listitem>
            <listitem>
                <para>
                    <command>make install</command> to install the software where you specified in <option>-DCMAKE_INSTALL_PREFIX</option>
                </para>
            </listitem>
        </orderedlist>
        <para>
            If your working directory is a clone of the repository with the appropriate branch checked out,
            here's an example of how to do all of this.
        </para>
        <example>
            <title>Building mesytec-mvlc with fribdaq-readout</title>
            <programlisting>
mkdir build
cd build
cmake .. -DCMAKE_INSTALL_PREFIX=/usr/opt/mesytec-mlc/0.1.0 -DNSCLDAQ_ROOT=/usr/opt/daq/12.1-007
(bunch of output here)
make -j12                     # Make for 12 cores.
(more output here)
make test                     #optional 
(more output here)
make install
(final bits of output here)
            </programlisting>
        </example>
    </section>
    <section>
        <title>Extending the generator with loadable C++ generators</title>
        <para>
            The mvlcgenrate program can, as the VMUSBReadout program, accept loadable
            C++ driver extensions.   These are modeled after lodable C++ extensions
            that VMUSBReadout uses in order to make it simpler to port existing code to
            the mvlcgenerate.
        </para>
        <para>
            A sample C++ driver extension, heavily commented, is provided in 
            <filename>$DAQSHARE/mvlc-examples/SampleDriver.cpp</filename>
            This section will go over.
            that example.  There are three parts to an mvlcgenerate plugin:
        </para>
        <orderedlist>
            <listitem><para>
                One or more driver classes.
            </para></listitem>
            <listitem><para>
                For each driver class a DriverCommand that needs to know how to 
                create a driver instance
            </para></listitem>
            <listitem><para>
                One initialization function that is called when the plugin is loaded,
                which needs to register the driver commands with the configuration 
                script parser.
            </para></listitem>
        </orderedlist>
        <section>
            <title>Some differences to keep in mind</title>
            <para>
                It's always important to keep in mind the main difference
                between VMUSBReadout daqconfig plugins and mvlcgenerate's
                plugins:  The mvlcgenerat plugins have no access to hardware.
                While there are <classname>CVMUSB</classname> and
                <classname>CVMUSBReadoutList</classname> classes, these only 
                record operations, rather than performing them. The
                <methodname>CVMUSB::executeList</methodname> methods are
                provided but they just cause the <classname>CVMUSB</classname>
                to add the objects in the <classname>CVMUSBReadoutList</classname>
                to the list of operations it is recording.
            </para>
            <para>
                The mvlcgenerate headers your driver might need
                are in the mvlc subdirectory of the 
                $DAQINC directory, however to avoid inadvertently 
                grabbing the wrong header you should specify
                -I$DAQINC rather than -I$DAQINC/mvlc in addition to
                -I$DAQINC
            </para>
            <para>
                Let's look at the headers our driver uses
            </para>
            <example>
                <title>Headers included by the sample MVLC loadable driver</title>
                <programlisting>
#include &lt;mvlc/CReadoutHardware.h&gt;
#include &lt;mvlc/CReadoutModule.h&gt;
#include &lt;mvlc/DeviceCommand.h&gt;
#include &lt;mvlc/CVMUSB.h&gt;
#include &lt;mvlc/CVMUSBReadoutList.h&gt;
#include &lt;mvlc/MVLCConfigParser.h&gt;
#include &lt;XXUSBConfigurableObject.h&gt;
#include &lt;stdint.h&gt;
#include &lt;tcl.h&gt;
#include &lt;TCLInterpreter.h&gt;                 
                </programlisting>
            </example>
            <para>
              Note that several of the headers included from the
              mvlc subdirectory have identical filenames for
              VMUSBReadout headers.  This is why it is important to
              be explicit about which header you need.
            </para>
        </section>
        <section>
            <title>Structure of a driver.</title>
        <para>
            Let's look at how a driver is built.  A driver is
            a class that is derived from 
            <classname>CReadoutHardware</classname> it is responsible
            for generating the lists of MVLC VME operations that
            will be performed at initialization, event read and end of run.
            Driver instances also have a configuration that is specified
            as a set of configuration keywords and validations for the
            values associated with those keywords.  The configuration
            is held in an <classname>XXUSB::CConfigurableObject</classname>
            see the definition  of this class for more information about
            the sorts of values that can be associated with configuration
            keywords.
        </para>
        <para>
            The example below is the class definition for the
            sample driver in 
            <filename>$DAQSHARE/mvlc-examples/SampleDriver.cpp</filename>
        </para>
        <example>
            <title>Sample mvlcgenerate driver definition</title>
            <programlisting>
class SampleDriver : public CReadoutHardware {
private:
    XXUSB::CConfigurableObject* m_pConfiguration;  // Pointer to config.
public:
    SampleDriver();
    ~SampleDriver();

    virtual void onAttach(XXUSB::CConfigurableObject&amp; configuration);
    virtual void Initialize(CVMUSB&amp; controller);
    virtual void addReadoutList(CVMUSBReadoutList&amp; list);
    virtual void onEndRun(CVMUSB&amp; interface);
};
            </programlisting>
        </example>
        <para>
          The subsections below will describe each of the methods in 
          the drier.  Note the <varname>m_pConfiguration</varname>
          member data.  This will be a pointer to the driver instance's
          configuration.  Its methods will be used both to define the
          configuration options the driver accepts and to interrogate
          the values of those options when it's time to generate code.
        </para>
        <section>
            <title>onAttach - define the configurable options</title>
            <para>
                <methodname>onAttach</methodname> is called when the driver is being 
                associated with its configuration object.  The configuration object is
                passed in by reference, but the caller retains ownership, so you shouild not delete
                this object.  Normally, you will need to save a pointer to that object, however.
            </para>
            <para>
                THe configuration object is an instance of <classname>XXUSB::CConfigurableObject</classname>
                This class has methods both for defining configuration options and interrogating
                their  current values.  The configuration maintained by <classname>XXUSB::CConfigurableObject</classname>
                consists of a set of keywords (called <firstterm>options</firstterm>), and associated values.
                A value proposed for an option can be validated by a validation function.  Many standard
                validators are supplied with <classname>XXUSB::CConfigurableObject</classname> as well
                as convenience methods to use them transparently.  By convention, option names, begin with
                a <literal>-</literal> to make them look like Tk options.  This is not enforced by 
                <classname>XXUSB::CConfigurableObject</classname>.
            </para>
            <para>
                The work of <methodname>onAttach</methodname> is to define thd configuration options the
                driver needs.  A minimal option might be the base address of the specific device the
                driver is generating code for (by convention this is <option>-base</option>).  
                Let's pull apart the sample driver's implementation.  See the sample driver's comments for
                <methodname>onAttache</methodname>
            </para>
            <example>
                <title>mvlcgenerate SampleDriver::onAttach - defining configuration options</title>
                <programlisting>
void
SampleDriver::onAttach(XXUSB::CConfigurableObject&amp; configuration) {
    m_pConfiguration = &amp;configuration;                        <co id='mvlc-sample-oa-saveconfig' />

    m_pConfiguration->addIntegerParameter("-marker", 0);          <co id='mvlc-sample-oa-marker' />


    m_pConfiguration->addIntegerParameter("-resetloc");           <co id='mvlc-sample-oa-resetloc' />
    m_pConfiguration->addIntegerParameter("-resetvalue", 0, 0xffff, 0xaaaa); <co id='sample-oa-resetval' />

    m_pConfiguration->addBooleanParameter("-writeendrun", false);  <co id='mvlc-sample-oa-endrun' />
    m_pConfiguration->addIntegerParameter("-endrunloc");
    m_pConfiguration->addIntegerParameter("-endrunvalue", 0, 0xffff, 0xbbbb);
}

                </programlisting>
            </example>
            <para>
                Before picking apart <methodname>onAttach</methodname> a bit about the operations the
                driver will generate.  For readout, the driver will generate a fixed marker.
                For initialization, the driver will generate a 16 bit write to some VME location
                that can be specified. For finalization (as the run ends), the driver will
                <emphasis>optionally</emphasis> generate a 16 bit write to another VME location.
            </para>
            <para>
                The numbers in the list below refer to the number in the example above.
            </para>
            <calloutlist>
                <callout arearefs='mvlc-sample-oa-saveconfig'><para>
                    The configuration does us no good if we cannot interrogate it.  This line
                    saves a pointer to the configuration object as instance data. 
                    The life-cycle of the driver instance is that in the configuration script,
                    it will be created via a <command>create</command> subcommand for the
                    driver command associated with it, the options configured via the
                    <command>config</command> subcommand and, once the script has executed,
                    the generated will invoke the <methodname>Initialize</methodname>,
                    <methodname>addReadoutList</methodname> and <methodname>onEndRun</methodname>
                    methods to generate operations to be performed.
                </para>
            </callout>
            <callout arearefs='mvlc-sample-oa-marker'><para>
                This call to one of the <classname>XXUSB::CConfigurableObject</classname>'s 
                <methodname>addIntegerParameter</methodname> adds an option named <option>-marker</option>.
                MVLC markers, unlike VMUSB markers are 32 bits wide.  Therefor, no range checking is 
                requested.  The <literal>0</literal> value is the initial value of this configuration 
                option.  If <option>-marker</option> is never configured in the configuration script,
                it will have the value <literal>0</literal> when queried.
            </para></callout>
            <callout arearefs='mvlc-sample-oa-resetloc'><para>
                This line, defines an option <option>-resetloc</option> that, once more, is an integer
                that is not range checked.  This will hold the VME addresss to be written to when
                generating code to initialize the device.  A more normal way to do this,
                would be to define an <option>-base</option> option, which would describe the base
                address of the module and then generate writes to appropriate offsets 
                relative to that base
                address.
            </para></callout>
            <callout arearefs='sample-oa-resetval'><para>
                This call to another overload of <methodname>addIntegerParameter</methodname> creates
                an integer option; <option>-resetvalue</option> that is range checked so that its
                value is within the range of values of a 16 bit number (between 0 and 0xffff).
                The default value of this optin is <literal>0xaaaa</literal>.
                Normally, options would provide meaningful options for how the device is configured
                (e.g. <option>-zerosuppress</option> might turn on or off a zero suppressed read), and
                the actual initialization sequence would be computed from those options.
                When designing a sample driver, think about how to provide options that make sense to the
                user rather than the device with default values that reflect the most common use of the
                device.  
            </para></callout>
            <callout arearefs='mvlc-sample-oa-endrun'><para>
                In this call to <methodname>addBooleanParameter</methodname> we define a 
                configuration option <option>-writeendrun</option> that is a boolean with an
                initial <literal>false</literal> value.  Valid values for boolean options,
                in your script are the values for boolean options that Tcl scripts accept.
                We will use the value of <option>-writendofrun</option> to determine if we will
                generate a write to execute in <methodname>onEndRun</methodname>.  As you can 
                probably guess by now, <option>-endrunloc</option> will, if <option>-writeendrun</option>
                is true, define the VME address at which the write will occur and <option>-endrunvalue</option>
                the value written.
            </para></callout>
         </calloutlist>
        </section>
        <section>
            <title>Initialize - generating operations to set-up the device</title>
            <para>
                <methodname>Initialize</methodname> is called after the configuration script
                has been completely executed.  It is only called if the configuration script
                executed without errors.   This method generates operations that will be
                performed as a run is starting to setup the device for data taking.
                Normally this invovle querying the configuration and performing operations
                on the <varname>controller</varname> parameter passed in.
            </para>
            <para>
                While the <classname>CVMUSB</classname> class in the mvlcgenerate framework is
                intended to be much like the class with the same name in the VMUSBReadout application,
                the main thing to keep in mind is that your driver cannot actually talk to hardware.
                It can only generate operations that will be memorized and regurgitated when
                it comes time to generate initialization operation lists.
            </para>
            <para>
                This has the following implications, if you are porting drivers from
                VMUSBReadout:
            </para>
            <itemizedlist>
                <listitem><para>
                    You cannot interrogate the device (by performing reads).  This may require additional
                    options to be defined.  For example, when porting the driver for the
                    CAEN V785, V775, V792, modules, the VMUSBReadout driver asked each module
                    for its model number and generated code specific to the module type, ADC, QDC, or TDC,
                    this is not possible with mvlcgenerate, so an additional option <option>-type</option>
                    was added that has an enumerated vavlue (<literal>adc</literal>, <literal>qdc</literal>,
                    or <literal>tdc</literal>) that drives module type specific code generatikon in mvlcgenerate.
                </para></listitem>
                <listitem><para>
                     Bit more subtle is that delays must be pushed into the set of generated operations.
                    In many drivers, initialization requires some delay.  For example, a general module
                    reset may require a delay before the module can accpet additional configuration.
                    In VMUSBReadout modules, this was often done via calls to <literal>sleep</literal> or
                    <literal>usleep</literal>.  In mvlcgenerate, this will simply delay code
                    generation rather than actually supplying the run-time delay required.  Instead you must
                    add a run-time delay using the <classname>CVMUSB</classname> <methodname>delay</methodname>
                    method.
                </para></listitem>
                <listitem><para>
                    While using <classname>CVMUSBReadoutList</classname> to generate list of operations is
                    supported for compatibility with existing VMUSBReadout drivers, the
                    <classname>CVMUSB</classname>::<methodname>executeList</methodname> methods,
                    just append the operations in the list to the list of VME operations it is memorizing.
                    This method will never generate an error and it will always claim to have read
                    <literal>0</literal> bytes of data.
                </para></listitem>
            </itemizedlist>
            <para>
                In our <classname>SampleDriver</classname> none of this is relevant, but it is important to
                keep these points in mind for actual drivers.  Let's have a look at the
                <classname>SampleDriver</classname>::<methodname>Initialize</methodname> code:
            </para>
            <example>
                <title>mvlc SampleDriver::Initialize - generating initialization operations.</title>
                <programlisting>
void
SampleDriver::Initialize(CVMUSB&amp; controller) {
    uint32_t addr = m_pConfiguration-&gt;getIntegerParameter("-resetloc");
    uint32_t value = m_pConfiguration-&gt;getIntegerParameter("-resetvalue");

    controller.vmeWrite16(addr, CVMUSBReadoutList::a32UserData, value);
}
                </programlisting>
            </example>
            <para>
                This code is relatively straightforward,   The value sof the two integer 
                configuration parameters, <option>-restloc</option> <option>-resetvalue</option>
                are fetched from the configuration object and used in a call to 
                the <classname>CVMUSB</classname> <methodname>vmewrite16</methodname> to add a 16
                bit VME write to the list of initialization opertaions.  
                <literal>CVMUSBReadoutList::a23UserData</literal> specifies a VME bus
                <firstterm>address modifier</firstterm> that specifies the VME address space
                associated with the address.  A discussion of VME address modifiers is beyond the 
                scope of this documentation. See:
                <ulink url='https://www.vita.com/page-1855177'>https://www.vita.com/page-1855177</ulink>
                for a description of address modifiers and what they do.
            </para>
        </section>
        <section>
            <title>AddReadoutList - Generating operations to read the device</title>
            <para>
                The <methodname>AddReadoutList</methodname> requires that you generate the list of operations
                that will be executed when the device is read out.  In the case of our sample driver,
                we're just going to add a maker to the list.  When coding this, or porting drivers from the
                VMUSBReadout, remember once more that you are not actually able to perform operations on the
                device itself.  You can only add operations that will be added to the list of operations that
                will read the device.  All read operations added to that list, will insert data into the 
                event buffer.
            </para>
            <para>
                <methodname>AddReadoutList</methodname> gets a reference to a <classname>CVMUSBReadoutList</classname>
                which is intended to implement the operations that the class of the same name implmeents for the
                VMUSBReadout framework.  Where there is a mismatch in how these operations are implemented
                between the MVLC and the VMUSB, the proper MVLC readout list code is generated to 
                perform the requested operation.   For example, block reads where the transfer count comes
                from a field ina  preceding read generate the appropriate accumluator load, shift and mask and
                accumulator driven block read.
            </para>
            <para>
                Here is the <methodname>addReadoutList</methodname>  for our sample driver:
            </para>
            <example>
                <title>mvlcgenerate SampleDriver::AddReadoutList - Generating readout operations.</title>
                <programlisting>
void
SampleDriver::addReadoutList(CVMUSBReadoutList&amp; list) {
    uint32_t markerValue = m_pConfiguration-&gt;getIntegerParameter("-marker");
    list.addMarker(markerValue);
}
                </programlisting>
            </example>
            <para>
                The only thing of note here is that the markers generated by the MVLC are 32 bit markers,
                while those generated by the VMUSB are 16 bit markers.
            </para>
        </section>
        <section>
            <title>onEndRun - shutting down the device</title>
            <para>
                <methodname>onEndOfRun</methodname> is called to give the driver an opportunity to
                perform operations as a data taking run ends.  Normally, these operations
                disable the device and clear any buffered data.
            </para>
            <example>
                <title>mvlcgenerate SampleDriver::addReadoutList - generating event readout</title>
                <programlisting>
void
SampleDriver::onEndRun(CVMUSB&amp; controller) {
    if (m_pConfiguration-&gt;getBoolParameter("-writeendrun")) {
        uint32_t addr = m_pConfiguration-&gt;getIntegerParameter("-endrunloc");
        uint32_t value = m_pConfiguration-&gt;getIntegerParameter("-endrunvalue");
        controller.vmeWrite16(addr, CVMUSBReadoutList::a32UserData, value);
    }
}

                </programlisting>
            </example>
            <para>
                Notice how we use the <option>-writendrun</option> to conditionalize the
                write.  Everything else should, by now be pretty clear.
            </para>
        </section>
        </section>
        <section>
            <title>The Driver command</title>
            <para>
                In order to be created and configured, a driver must have an associted Tcl command
                extension.  The bulk of the operations performed by this extension are the same
                no matter which type of device is managed.     The <classname>DeviceCommand</classname>
                is a Tcl command extension class that implements all of the code needed to
                create, configure, and introspect the configuration of a device <emphasis>except</emphasis>
                the actual device creation.
            </para>
            <para>
                Here is the sample device's device command class definition:
            </para>
            <example>
                <title>mvlcgenerate SampleDriverCommand definition</title>
                <programlisting>
class SampleDriverCommand : public DeviceCommand {
public:
    SampleDriverCommand(CTCLInterpreter&amp; interp, TCLConfigParser&amp; parser);
    ~SampleDriverCommand();

protected:

    virtual CReadoutModule* createDevice(std::string name);    

};

                </programlisting>
            </example>
            <para>
                The key methods are the constructor, which will give a name to the command,
                and the <methodname>createDevice</methodname> which will create an actual
                device instance.
            </para>
            <section>
            <title>mvlcgenerate SampleDriverCommand construction/destruction</title>
            <para>
                Let's have a look at the constructor of the <classname>SampleDriverCommand</classname>:
            </para>
            <example>
                <title>mvlcgenerate SampleDriverCommmand constructor - defining the command</title>
                <programlisting>
SampleDriverCommand::SampleDriverCommand(
    CTCLInterpreter&amp; interp, TCLConfigParser&amp; parser) :
    DeviceCommand(interp, "example", parser)
{}

                </programlisting>
            </example>
            <para>
                In our case, the base class contructor does all the work.  We passed the interpreter,
                a command name: <literal>example</literal> and the configuration parser on to it.
                The Configuration parser (<classname>TCLConfigPaser</classname>) is the object that
                accumulates the configuration by processing the configuration script.
                Some drivers and driver commands, may require access to this object.  For example,
                the <literal>caenchain</literal> object must generate objects that can  validate
                that the modules in their chains actually exist and are <classname>C785</classname>
                modules.  The parser, includes factilities for finding modules or, alternatively, 
                determining that modules don't exist.  
            </para>
            <para>
                The destructor, in our case, is empty.  If your command allocates any dynamic
                memory, of course the destructor must delete it.
            </para>
            </section>
            <section>
                <title>createDevice - creating a device instance</title>
                <para>
                    The <methodname>createDevice</methodname> is respsonsible for
                    creating a new, dynamically allocated, driver instance 
                    (<classname>SampleDriver</classname> in our case), and binding it into a
                    <classname>CReadoutModule</classname>.  Remember, a device instance, consists of
                    a driver instance and a configuration.  These are packaged together in a 
                    <classname>CReadoutModule</classname> object.
                </para>
                <para>
                    For simple devices, the boilerplate below can be modified to create and return
                    an appropriate <classname>CReaodutModule</classname>. Note that once returned,
                    ownership of the module and associated device pass to the caller.
                </para>
                <example>
                    <title>mvlcgenerate - creating a device instance (createDevice).</title>
                    <programlisting>
CReadoutModule*
SampleDriverCommand::createDevice(std::string name) {
    CReadoutModule* result = new CReadoutModule;
    result-&gt;SetDriver(new SampleDriver);

    return result;
}

                    </programlisting>
                </example>
                <para>
                    Once a <classname>CRedoutModule</classname> is created its driver can be
                    allocated and associated using the <methodname>SetDriver</methodname>
                    method.  The <classname>CReadoutModule</classname> is then returned to the
                    caller, in our case, the code executing the 
                    <command>example create <replaceable>name</replaceable></command>
                    example.  The <parameter>name</parameter> is the name of the device instance
                    being created.  In most cases, the driver instance doesn't need this.
                </para>
            </section>
            </section>
            <section>
                <title>Initialization code</title>
                <para>
                    When the Tcl <command>load</command> is used to load a shared object into memory,
                    it runs the an initialization function.  This function must have the name
                    <emphasis>prefix</emphasis>_Init,and is passed the <type>Tcl_Interp*</type>
                    for the interpreter running the <command>load</command>.
                </para>
                <para>
                    The prefix in the function name is constructed by taking the library name,
                    removing the <literal>lib</literal> and the extension, capitalizaing the first letter,
                    and making all other letter lower case.  For example, if we built our
                    driver into the the file:  <filename>libSampleDriver.so</filename> the
                    prefix would be <literal>Sampledriver</literal> and the initialization
                    function would be callsed <literal>Sampledriver_Init</literal>
                </para>
                <para>
                    Let's have a look at  <literal>Sampledriver_Init</literal>
                </para>
                <example>
                    <title>mvlcgenerate sample driver initialization code:</title>
                    <programlisting>
 extern "C" {                                                           <co id='mvlcgen-sd-init-cbinding' />
    int Sampledriver_Init(Tcl_Interp* pInterp) {

        TCLConfigParser*  parser = TCLConfigParser::getInstance();    <co id='mvlcgen-sd-init-parserinstance' />
        CTCLInterpreter* interp  = parser-&gt;getInterpreter();          <co id='mvlcgen-sd-init-interpencap' />
        parser-gt;addExtension(new SampleDriverCommand(*interp, *parser)); <co id='mvlcgen-sd-init-addext' />

        return TCL_OK;                                               <co id='mvlcgen-sd-init-success' />
    }
 }                   
                    </programlisting>
                </example>
                <para>
                    Let's pick this all apart line by line:
                </para>
                <calloutlist>
                    <callout arearefs='mvlcgen-sd-init-cbinding'><para>
                        C++ achieves function, method and operator overloading by <firstterm>name mangling</firstterm>.
                        Name mangling means that the actual name of a function, as seen by the linker, is
                        the name you give it, modified in ways that describe the parameters and, if appropriate,
                        the class or namespace the function lives in.
                        For example, the SampleDriver::createDevicde method winds up crating a function
                        named: 
                        <literal>_ZN19SampleDriverCommand12createDeviceENSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEE</literal>.
                        </para>
                        <para>
                            <literal>extern "C" {}</literal> are blocks in which the C++ compiler
                            turns off this name manging and, instead, generates external function names
                            that are compatible with those used by <literal>C</literal> rather than C++.
                            This is necessary if our <function>Sampledriver_Init</function> function name
                            is going to be found by the Tcl <command>load</command> command.
                    </para></callout>
                    <callout arearefs='mvlcgen-sd-init-parserinstance'><para>
                        We will need the configuration parser (the object interpreting the configuration
                        script), to pass it to the constructor of our command.  The
                        <classname>TCLConfigParser</classname> is, essentially a singleton and
                        its <methodname>getInstance</methodname> static method returns a pointer to
                        it.
                    </para></callout>
                    <callout arearefs='mvlcgen-sd-init-interpencap'><para>
                        The parameter to our initialization function is a raw Tcl interpreter pointer.
                        To use the Tcl++ library we must must have a <classname>CTCLInterpreter</classname>
                        The <methodname>getInterpreter</methodname> Of the <classname>TCLConfigParser</classname>
                        running the script gives us that.
                    </para></callout>
                    <callout arearefs='mvlcgen-sd-init-addext'><para>
                        This operation creates a new <classname>SampleDriver</classname> command and
                        makes it know to the driver.  Making it know to the driver allows the driver
                        to destroy the command when the parser is destroyed.  
                    </para></callout>
                    <callout arearefs='mvlcgen-sd-init-success'><para>
                        The Tcl <command>load</command> command expects a  status return.  Returning
                        <literal>TCL_OK</literal> tells the command initialization was successful.
                        In the event of an error you should set the result and return a <literal>TCL_ERROR</literal>
                        e.g. like this:
                        <informalexample>
                            <programlisting>
        interp-&gt;setResult("Some useful error message");
        return TCL_ERROR;
                            </programlisting>
                        </informalexample>
                    </para></callout>
                </calloutlist>
            </section>
        <section>
            <title>Building the C++ extension and using it.</title>
            <para>
                To build the driver, first start the container an setup the environment variables for
                the version of FRIB/NSCLDAQ you will be using via the appropriate <filename>dasetup.bash</filename>.
                Given the file <filename>SampleDriver.cpp</filename> is in the default directory; This
                command builds it into <filename>libSampleDriver.so</filename> in the default directory:
            </para>
            <example>
                <title>mvlcgenerate building the C++ extension:</title>
                <programlisting>
 g++ -olibSampleDriver.so -shared -fPIC \
     SampleDriver.cpp \
     -L$DAQLIB -lmvlcGenerator -Wl,-rpath=$DAQLIB \
     -I$DAQINC -I/usr/include/tcl8.6
                </programlisting>
            </example>
            <para>
                If future containers use a different version of Tcl, you may need to change the
                directory from which <filename>tcl.h</filename> is included.
            </para>
            <para>
                If we run mvlcgenerate in the same directory, this configuration file
                fragment shows how to use the sample driver to create and configure an instance of that
                "device".
            </para>
            <example>
                <title>mvlcgenerate - using a C++ extension driver:</title>
                <programlisting>
load [file join . libSampleDriver.so]                               <co id='mvlcget-sd-use-load' />

example create sample                                               <co id='mvlcgen-sd-use-createconfig' />
example config sample -marker 0x1212 -resetloc 0x12340000 -resetvalue 0x1234
example config sample -writeendrun true -endrunloc 0x12344000 -endrunvalue 0x4321

...                                                                 <co id='mvlcget-sd-use-stack' />
                </programlisting>
            </example>
            <calloutlist>
                <callout arearefs='mvlcget-sd-use-load'><para>
                    This loads the driver library making the <command>example</command> available.
                    If your driver is not located in the same directory that you are running
                    mvlcgenerate from, replace the <literal>.</literal> with the path to the directory it is in.
                    In general <command>load</command> will need a full path as otherwise only the system
                    shared library load paths will be searched for the file.
                </para></callout>
                <callout arearefs='mvlcgen-sd-use-createconfig'><para>
                    These three lines show how the <command>example</command> can be used to
                    create and configure instances of the sample driver just like any built in command can.
                </para></callout>
                <callout arearefs='mvlcget-sd-use-stack'><para>
                    Naturally, once created and configured, driver instances must be added to stacks
                    to be used.  At some point, in this example, a stack has to be crerated (event or scaler),
                    which has <literal>sample</literal>  in the list passed to its <option>-modules</option>
                    option.
                </para></callout>
            </calloutlist>
        
        </section>
    </section>
</chapter>
<!-- /chapter -->
